WIP Testing Deployment

## `deploy-terraform` runs on ansible

- [ ] A fresh install with no pre-defined datasets should run
  - [ ] `npm run package` should run without errors (requires running `init-netcreate-config.js` on newly created droplet -- see `nc-app-config`)
