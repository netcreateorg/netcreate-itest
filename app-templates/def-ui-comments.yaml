# metadata
_schemaVersion: '2.0'
_key: _object_types

# declare complex types (PascalCase)
CommentType:
  type: object
  slug.type: id
  label.type: string
  prompts:
    type: object
    format.type: string
    prompt.type:: string
    help.type: string
    feedback.type: string
